[project]
name = "trailheadparking"
version = "0.1.0"
description = "Static site builder for trailheadparking.com"
requires-python = ">=3.12"
dependencies = [
    "sitekit",
]

[tool.uv.sources]
sitekit = { path = "../sitekit", editable = true }
